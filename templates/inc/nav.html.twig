{% set ADMIN = 'ROLE_ADMIN' %}

<div class="menu_container">
    <a href="{{ path('main_index') }}"><img class="logo" src="{{ asset('img/logo_eni.png') }}" alt="Sortir"></a>

    <nav>

        {% if app.user %}

            <ul>
                <li><a href="{{ path('main_index') }}">Accueil</a></li>
                {% if is_granted(ADMIN) %}
                    <li><a href="{{ path('city_list') }}">Villes</a></li>
                    <li><a href="{{ path('site_list') }}">Campus</a></li>
                {% endif %}
                <li><a href="{{ path('user_profile', {'id': app.user.id}) }}">Mon profil</a></li>
                <li><a href="{{ path('app_logout') }}">Se déconnecter</a></li>

            </ul>
        {% endif %}
    </nav>
</div>

