{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Accueil{% endblock %}

{% block body %}
    <h1>Page d'accueil</h1>
    <div>
        <p>Date du jour : {{ "now" | date('d/m/Y') }}</p>
        {% if app.user %}
            <p>Participant : {{ app.user.nickname }}</p>
        {% else %}
            <p>Participant : Non connecté(e)</p>
        {% endif %}
    </div>
    {# TODO: To list the trips with sorting option #}
{% endblock %}
